import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function NotesScreen() {
  return (
    <ScreenPlaceholder
      title="Notes"
      description="Highlight verses and keep personal notes as you read. Private to you by default."
    />
  );
}
