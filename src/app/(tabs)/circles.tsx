import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function CirclesScreen() {
  return (
    <ScreenPlaceholder
      title="Circles"
      description="Post updates and read as a group. Belong to as many circles as you like — a whole-church circle plus your own small groups."
    />
  );
}
