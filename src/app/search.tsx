import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function SearchScreen() {
  return (
    <ScreenPlaceholder
      title="Search"
      description="Instant full-text search across every bundled translation, powered by on-device SQLite FTS5 — no network required."
    />
  );
}
