import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Profile"
      description="Sign in, manage your circles, and adjust reading preferences."
    />
  );
}
